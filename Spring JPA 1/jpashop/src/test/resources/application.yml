spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 db 사용
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # show_sql: true //얘는 sout 출력해서 안씀
#        format_sql: true

# boot db 설정없으면 메모리 db로 돌려줌

logging.level:
  org.hibernate.SQL: debug
  # org.hibernate.type: trace
